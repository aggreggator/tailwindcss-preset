const system = {
  /** Basic config */
  // prefix: '',
  // important: false,
  // separator: ':',
  /** Important: Utility CSS needs purging */
  purge: {
    // enabled: false,
    content: ['./src/**/*.tsx'],
  },
};

module.exports = { system };
